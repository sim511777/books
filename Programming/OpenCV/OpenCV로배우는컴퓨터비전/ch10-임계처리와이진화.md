# 10장. 임계처리와 이진화

[◀ 이전: 9장. 필터링과 블러](ch09-필터링과블러.md) | [📖 목차](00-목차.md) | [다음: 11장. 모폴로지 연산 ▶](ch11-모폴로지연산.md)


9장에서는 이미지를 부드럽게 만들거나 선명하게 만드는 필터링을 다뤘습니다. 이번 장에서는 이미지를 아예 흑과 백, 두 가지 값만 가지는 이진 이미지(binary image)로 바꾸는 임계처리(thresholding)를 살펴봅니다. 임계처리는 문서에서 글자만 골라내거나, 배경에서 물체를 분리하거나, 이후 13장에서 다룰 컨투어 검출의 입력을 준비하는 등 컴퓨터 비전 파이프라인 곳곳에서 사용되는 기본 기술입니다.

## 10.1 단순 임계처리: threshold

임계처리의 가장 단순한 형태는 픽셀 값을 하나의 기준값(임계값, threshold)과 비교해서 그보다 크면 한 값으로, 작으면 다른 값으로 바꾸는 것입니다. OpenCV는 이를 `threshold` 함수로 제공합니다.

```
retval, dst = cv2.threshold(src, thresh, maxval, type)
```

`src`는 보통 그레이스케일 이미지, `thresh`는 임계값, `maxval`은 조건을 만족했을 때 부여할 값, `type`은 어떤 방식으로 비교할지를 결정하는 플래그입니다. 대표적인 플래그는 다음과 같습니다.

| 플래그 | 동작 |
|---|---|
| `THRESH_BINARY` | 픽셀 값이 `thresh`보다 크면 `maxval`, 아니면 0 |
| `THRESH_BINARY_INV` | `THRESH_BINARY`의 반대: 크면 0, 아니면 `maxval` |
| `THRESH_TRUNC` | `thresh`보다 크면 `thresh`로 자르고, 작으면 원래 값 유지 |
| `THRESH_TOZERO` | `thresh`보다 크면 원래 값 유지, 작으면 0 |
| `THRESH_TOZERO_INV` | `THRESH_TOZERO`의 반대: 크면 0, 작으면 원래 값 유지 |

`THRESH_BINARY`는 밝은 물체를 흰색으로, 어두운 배경을 검은색으로 만드는 가장 흔한 용도에 쓰이고, `THRESH_BINARY_INV`는 반대로 어두운 물체를 강조하고 싶을 때(예: 흰 종이 위의 검은 글자) 사용합니다.

### 예제 10-1. THRESH_BINARY와 THRESH_BINARY_INV 비교

#### Python

```python
import cv2

img = cv2.imread('document.jpg', cv2.IMREAD_GRAYSCALE)
if img is None:
    raise FileNotFoundError('document.jpg를 찾을 수 없습니다.')

thresh_val = 127
_, binary = cv2.threshold(img, thresh_val, 255, cv2.THRESH_BINARY)
_, binary_inv = cv2.threshold(img, thresh_val, 255, cv2.THRESH_BINARY_INV)
_, trunc = cv2.threshold(img, thresh_val, 255, cv2.THRESH_TRUNC)
_, tozero = cv2.threshold(img, thresh_val, 255, cv2.THRESH_TOZERO)

cv2.imshow('Original (gray)', img)
cv2.imshow('THRESH_BINARY', binary)
cv2.imshow('THRESH_BINARY_INV', binary_inv)
cv2.imshow('THRESH_TRUNC', trunc)
cv2.imshow('THRESH_TOZERO', tozero)
cv2.waitKey(0)
cv2.destroyAllWindows()
```

#### C++

```cpp
#include <opencv2/opencv.hpp>

int main() {
    cv::Mat img = cv::imread("document.jpg", cv::IMREAD_GRAYSCALE);
    if (img.empty()) {
        CV_Error(cv::Error::StsError, "document.jpg를 찾을 수 없습니다.");
    }

    double threshVal = 127;
    cv::Mat binary, binaryInv, trunc, tozero;
    cv::threshold(img, binary, threshVal, 255, cv::THRESH_BINARY);
    cv::threshold(img, binaryInv, threshVal, 255, cv::THRESH_BINARY_INV);
    cv::threshold(img, trunc, threshVal, 255, cv::THRESH_TRUNC);
    cv::threshold(img, tozero, threshVal, 255, cv::THRESH_TOZERO);

    cv::imshow("Original (gray)", img);
    cv::imshow("THRESH_BINARY", binary);
    cv::imshow("THRESH_BINARY_INV", binaryInv);
    cv::imshow("THRESH_TRUNC", trunc);
    cv::imshow("THRESH_TOZERO", tozero);
    cv::waitKey(0);
    return 0;
}
```

#### C#

```csharp
using OpenCvSharp;

using Mat img = Cv2.ImRead("document.jpg", ImreadModes.Grayscale);
if (img.Empty())
    throw new FileNotFoundException("document.jpg를 찾을 수 없습니다.");

double threshVal = 127;
using Mat binary = new Mat();
using Mat binaryInv = new Mat();
using Mat trunc = new Mat();
using Mat tozero = new Mat();

Cv2.Threshold(img, binary, threshVal, 255, ThresholdTypes.Binary);
Cv2.Threshold(img, binaryInv, threshVal, 255, ThresholdTypes.BinaryInv);
Cv2.Threshold(img, trunc, threshVal, 255, ThresholdTypes.Trunc);
Cv2.Threshold(img, tozero, threshVal, 255, ThresholdTypes.ToZero);

Cv2.ImShow("Original (gray)", img);
Cv2.ImShow("THRESH_BINARY", binary);
Cv2.ImShow("THRESH_BINARY_INV", binaryInv);
Cv2.ImShow("THRESH_TRUNC", trunc);
Cv2.ImShow("THRESH_TOZERO", tozero);
Cv2.WaitKey(0);
```

`threshold`는 사용한 실제 임계값을 첫 번째 반환값(`retval`)으로 돌려줍니다. 지금까지는 직접 127을 지정했지만, 항상 127이 최적의 값이라는 보장은 없습니다. 이미지마다 밝기 분포가 다르기 때문입니다.

## 10.2 Otsu의 방법: 임계값을 자동으로 찾기

적절한 임계값을 사람이 하나씩 눈으로 확인하며 찾는 것은 비효율적입니다. Otsu의 방법(Otsu's method)은 이미지의 히스토그램(14장에서 자세히 다룹니다)을 분석해서, 이미지를 두 그룹(배경과 전경)으로 나눌 때 두 그룹 내부의 분산은 최소화되고 두 그룹 사이의 분산은 최대화되는 임계값을 자동으로 계산해 줍니다.

사용법은 `threshold`에 `THRESH_OTSU` 플래그를 추가로 지정하는 것뿐입니다. 이때 `thresh` 매개변수로 넘긴 값은 무시되고 자동으로 계산된 값이 대신 사용되므로, 관례상 0을 넘겨줍니다. `THRESH_BINARY`와 `THRESH_OTSU`는 비트 OR(`|`)로 함께 지정합니다.

Otsu의 방법은 이미지의 밝기 분포가 배경과 전경, 두 개의 봉우리(bimodal)로 뚜렷하게 나뉘어 있을 때 특히 잘 동작합니다.

### 예제 10-2. Otsu의 방법으로 임계값 자동 결정하기

#### Python

```python
import cv2

img = cv2.imread('document.jpg', cv2.IMREAD_GRAYSCALE)

# 노이즈가 있으면 Otsu의 결과가 흔들릴 수 있어 가벼운 블러를 먼저 적용합니다.
blurred = cv2.GaussianBlur(img, (5, 5), 0)

otsu_thresh, otsu_binary = cv2.threshold(
    blurred, 0, 255, cv2.THRESH_BINARY | cv2.THRESH_OTSU
)

print(f'Otsu가 찾은 임계값: {otsu_thresh}')

cv2.imshow('Original', img)
cv2.imshow('Otsu Binary', otsu_binary)
cv2.waitKey(0)
cv2.destroyAllWindows()
```

#### C++

```cpp
#include <opencv2/opencv.hpp>
#include <iostream>

int main() {
    cv::Mat img = cv::imread("document.jpg", cv::IMREAD_GRAYSCALE);

    cv::Mat blurred;
    cv::GaussianBlur(img, blurred, cv::Size(5, 5), 0);

    cv::Mat otsuBinary;
    double otsuThresh = cv::threshold(
        blurred, otsuBinary, 0, 255, cv::THRESH_BINARY | cv::THRESH_OTSU
    );

    std::cout << "Otsu가 찾은 임계값: " << otsuThresh << std::endl;

    cv::imshow("Original", img);
    cv::imshow("Otsu Binary", otsuBinary);
    cv::waitKey(0);
    return 0;
}
```

#### C#

```csharp
using OpenCvSharp;

using Mat img = Cv2.ImRead("document.jpg", ImreadModes.Grayscale);

using Mat blurred = new Mat();
Cv2.GaussianBlur(img, blurred, new Size(5, 5), 0);

using Mat otsuBinary = new Mat();
double otsuThresh = Cv2.Threshold(
    blurred, otsuBinary, 0, 255, ThresholdTypes.Binary | ThresholdTypes.Otsu
);

Console.WriteLine($"Otsu가 찾은 임계값: {otsuThresh}");

Cv2.ImShow("Original", img);
Cv2.ImShow("Otsu Binary", otsuBinary);
Cv2.WaitKey(0);
```

## 10.3 적응형 임계처리: adaptiveThreshold

`threshold`와 Otsu의 방법은 모두 이미지 전체에 대해 단 하나의 임계값을 사용합니다. 그런데 조명이 균일하지 않은 이미지, 예를 들어 한쪽은 밝고 다른 쪽은 그림자가 진 문서 사진이라면 어떨까요? 전체에 적용할 하나의 임계값으로는 밝은 영역과 어두운 영역을 동시에 만족시킬 수 없습니다. 밝은 쪽에 맞춰 임계값을 낮추면 어두운 쪽 글자가 몽땅 사라지고, 어두운 쪽에 맞춰 임계값을 높이면 밝은 쪽 배경까지 글자로 인식되어 버립니다.

적응형 임계처리(`adaptiveThreshold`)는 이 문제를 픽셀마다 서로 다른 임계값을 적용하는 방식으로 해결합니다. 각 픽셀의 임계값을 그 픽셀 주변의 작은 영역(블록)에서 계산한 통계값을 기준으로 결정하기 때문에, 이미지의 왼쪽과 오른쪽이 밝기가 크게 다르더라도 각자의 지역적인 밝기 기준에 맞춰 이진화됩니다.

```
dst = cv2.adaptiveThreshold(src, maxValue, adaptiveMethod, thresholdType, blockSize, C)
```

- `adaptiveMethod`: 지역 임계값을 계산하는 방법. `ADAPTIVE_THRESH_MEAN_C`는 블록 내 픽셀의 단순 평균을, `ADAPTIVE_THRESH_GAUSSIAN_C`는 가우시안 가중 평균을 사용합니다.
- `blockSize`: 임계값을 계산할 지역 영역의 한 변 길이(홀수).
- `C`: 계산된 지역 평균에서 추가로 빼주는 상수. 결과가 너무 밝거나 어둡게 나올 때 미세 조정하는 용도입니다.

### 예제 10-3. 불균일한 조명에서 adaptiveThreshold 사용하기

#### Python

```python
import cv2

img = cv2.imread('uneven_light_document.jpg', cv2.IMREAD_GRAYSCALE)

# 전역 임계처리: 조명이 불균일하면 한쪽이 뭉개지기 쉽습니다.
_, global_binary = cv2.threshold(img, 0, 255, cv2.THRESH_BINARY | cv2.THRESH_OTSU)

# 적응형 임계처리: 지역별로 다른 임계값을 적용합니다.
adaptive_mean = cv2.adaptiveThreshold(
    img, 255, cv2.ADAPTIVE_THRESH_MEAN_C, cv2.THRESH_BINARY,
    blockSize=25, C=10
)
adaptive_gaussian = cv2.adaptiveThreshold(
    img, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C, cv2.THRESH_BINARY,
    blockSize=25, C=10
)

cv2.imshow('Global Otsu', global_binary)
cv2.imshow('Adaptive Mean', adaptive_mean)
cv2.imshow('Adaptive Gaussian', adaptive_gaussian)
cv2.waitKey(0)
cv2.destroyAllWindows()
```

#### C++

```cpp
#include <opencv2/opencv.hpp>

int main() {
    cv::Mat img = cv::imread("uneven_light_document.jpg", cv::IMREAD_GRAYSCALE);

    cv::Mat globalBinary;
    cv::threshold(img, globalBinary, 0, 255, cv::THRESH_BINARY | cv::THRESH_OTSU);

    cv::Mat adaptiveMean, adaptiveGaussian;
    cv::adaptiveThreshold(
        img, adaptiveMean, 255, cv::ADAPTIVE_THRESH_MEAN_C,
        cv::THRESH_BINARY, 25, 10
    );
    cv::adaptiveThreshold(
        img, adaptiveGaussian, 255, cv::ADAPTIVE_THRESH_GAUSSIAN_C,
        cv::THRESH_BINARY, 25, 10
    );

    cv::imshow("Global Otsu", globalBinary);
    cv::imshow("Adaptive Mean", adaptiveMean);
    cv::imshow("Adaptive Gaussian", adaptiveGaussian);
    cv::waitKey(0);
    return 0;
}
```

#### C#

```csharp
using OpenCvSharp;

using Mat img = Cv2.ImRead("uneven_light_document.jpg", ImreadModes.Grayscale);

using Mat globalBinary = new Mat();
Cv2.Threshold(img, globalBinary, 0, 255, ThresholdTypes.Binary | ThresholdTypes.Otsu);

using Mat adaptiveMean = new Mat();
using Mat adaptiveGaussian = new Mat();

Cv2.AdaptiveThreshold(
    img, adaptiveMean, 255, AdaptiveThresholdTypes.MeanC,
    ThresholdTypes.Binary, 25, 10
);
Cv2.AdaptiveThreshold(
    img, adaptiveGaussian, 255, AdaptiveThresholdTypes.GaussianC,
    ThresholdTypes.Binary, 25, 10
);

Cv2.ImShow("Global Otsu", globalBinary);
Cv2.ImShow("Adaptive Mean", adaptiveMean);
Cv2.ImShow("Adaptive Gaussian", adaptiveGaussian);
Cv2.WaitKey(0);
```

결과를 비교하면 전역 Otsu 이진화는 조명이 어두운 영역의 글자를 통째로 날려버리거나 밝은 영역을 잡음으로 채우는 반면, 적응형 임계처리는 두 영역 모두에서 글자를 비교적 온전하게 살려냅니다. `blockSize`가 너무 작으면 지역 잡음에 민감해지고, 너무 크면 전역 임계처리와 비슷해지므로 이미지의 글자 크기나 조명 변화 폭에 맞춰 실험적으로 조정하는 것이 좋습니다.

## 10.4 실전 예제: 문서 스캔 이미지 이진화하기

지금까지 배운 내용을 종합해서, 휴대폰 카메라로 찍은 손글씨나 문서 이미지를 텍스트와 배경이 뚜렷하게 구분되는 이진 이미지로 변환하는 실전 파이프라인을 만들어 보겠습니다. 일반적으로 다음 순서를 따릅니다.

1. 그레이스케일로 변환한다.
2. 노이즈를 줄이기 위해 가벼운 블러(가우시안 또는 미디안)를 적용한다.
3. 조명이 균일하다면 Otsu, 불균일하다면 적응형 임계처리를 적용한다.
4. 필요하다면 결과를 반전시켜 글자를 흰색(전경)으로 맞춘다.

### 예제 10-4. 문서 이미지에서 글자만 분리하기

#### Python

```python
import cv2

def binarize_document(path, uneven_light=True):
    img = cv2.imread(path, cv2.IMREAD_GRAYSCALE)
    if img is None:
        raise FileNotFoundError(f'{path}를 찾을 수 없습니다.')

    # 1. 스캔 노이즈 완화 (미디안 블러는 종이의 얼룩이나 점 잡음에 강합니다.)
    denoised = cv2.medianBlur(img, 3)

    # 2. 조명 상태에 따라 적응형 또는 Otsu 이진화 선택
    if uneven_light:
        binary = cv2.adaptiveThreshold(
            denoised, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
            cv2.THRESH_BINARY_INV, blockSize=31, C=15
        )
    else:
        _, binary = cv2.threshold(
            denoised, 0, 255, cv2.THRESH_BINARY_INV | cv2.THRESH_OTSU
        )

    return binary

result = binarize_document('handwritten_note.jpg', uneven_light=True)
cv2.imshow('Binarized Document', result)
cv2.waitKey(0)
cv2.destroyAllWindows()
```

#### C++

```cpp
#include <opencv2/opencv.hpp>

cv::Mat binarizeDocument(const std::string& path, bool unevenLight = true) {
    cv::Mat img = cv::imread(path, cv::IMREAD_GRAYSCALE);
    if (img.empty()) {
        CV_Error(cv::Error::StsError, path + "를 찾을 수 없습니다.");
    }

    cv::Mat denoised;
    cv::medianBlur(img, denoised, 3);

    cv::Mat binary;
    if (unevenLight) {
        cv::adaptiveThreshold(
            denoised, binary, 255, cv::ADAPTIVE_THRESH_GAUSSIAN_C,
            cv::THRESH_BINARY_INV, 31, 15
        );
    } else {
        cv::threshold(
            denoised, binary, 0, 255, cv::THRESH_BINARY_INV | cv::THRESH_OTSU
        );
    }
    return binary;
}

int main() {
    cv::Mat result = binarizeDocument("handwritten_note.jpg", true);
    cv::imshow("Binarized Document", result);
    cv::waitKey(0);
    return 0;
}
```

#### C#

```csharp
using OpenCvSharp;

static Mat BinarizeDocument(string path, bool unevenLight = true)
{
    Mat img = Cv2.ImRead(path, ImreadModes.Grayscale);
    if (img.Empty())
        throw new FileNotFoundException($"{path}를 찾을 수 없습니다.");

    using Mat denoised = new Mat();
    Cv2.MedianBlur(img, denoised, 3);

    Mat binary = new Mat();
    if (unevenLight)
    {
        Cv2.AdaptiveThreshold(
            denoised, binary, 255, AdaptiveThresholdTypes.GaussianC,
            ThresholdTypes.BinaryInv, 31, 15
        );
    }
    else
    {
        Cv2.Threshold(
            denoised, binary, 0, 255, ThresholdTypes.BinaryInv | ThresholdTypes.Otsu
        );
    }

    img.Dispose();
    return binary;
}

using Mat result = BinarizeDocument("handwritten_note.jpg", true);
Cv2.ImShow("Binarized Document", result);
Cv2.WaitKey(0);
```

여기서는 `THRESH_BINARY_INV`를 사용해 글자(원래 어두운 픽셀)가 흰색(255)으로, 배경(원래 밝은 픽셀)이 검은색(0)으로 나오도록 했습니다. 이렇게 전경을 흰색으로 맞춰두면 13장에서 배울 `findContours`가 글자 영역을 훨씬 자연스럽게 검출할 수 있습니다.

## 10.5 다음 장 예고: 모폴로지 연산과의 조합

실전에서 이진화 결과를 그대로 사용하는 경우는 많지 않습니다. 스캔 과정의 작은 잡음이 점처럼 남아 있거나, 글자의 획이 얇게 끊어져 보이는 경우가 흔하기 때문입니다. 11장에서 배울 모폴로지 연산(팽창, 침식, 열림, 닫힘)은 바로 이런 이진 이미지의 잡티를 정리하고 끊어진 획을 이어주는 후처리 단계로, 임계처리와 거의 항상 짝을 이루어 사용됩니다. 예를 들어 이번 장의 `binarize_document` 결과에 열림 연산을 적용하면 작은 점 잡음이 사라지고, 닫힘 연산을 적용하면 끊어진 글자의 획이 이어지는 것을 다음 장에서 직접 확인해 보겠습니다.

## 요약

- `threshold`는 픽셀 값을 하나의 전역 임계값과 비교해 이진 이미지를 만들며, `THRESH_BINARY`, `THRESH_BINARY_INV`, `THRESH_TRUNC`, `THRESH_TOZERO` 등의 옵션으로 비교 방식을 바꿀 수 있습니다.
- Otsu의 방법(`THRESH_OTSU`)은 이미지의 밝기 분포를 분석해 최적의 전역 임계값을 자동으로 찾아주며, 배경과 전경이 뚜렷이 구분되는 이미지에서 효과적입니다.
- `adaptiveThreshold`는 픽셀 주변의 작은 영역마다 서로 다른 임계값을 적용해, 조명이 불균일한 이미지에서 전역 임계처리보다 훨씬 안정적인 결과를 냅니다.
- 문서나 손글씨 이미지를 이진화할 때는 그레이스케일 변환 → 노이즈 제거 → (전역 또는 적응형) 임계처리의 순서로 파이프라인을 구성하는 것이 일반적입니다.
- 이진화 결과는 11장의 모폴로지 연산과 결합해 잡음을 제거하거나 끊어진 획을 잇는 후처리를 거치는 경우가 많습니다.

## 연습문제

1. `THRESH_BINARY`와 `THRESH_BINARY_INV`의 결과가 어떻게 다른지, 그리고 흰 종이 위의 검은 글자를 흰색 전경으로 만들고 싶을 때 어떤 플래그를 선택해야 하는지 설명해 보세요.
2. Otsu의 방법이 임계값을 자동으로 찾는 기준(그룹 내 분산과 그룹 간 분산)을 자신의 언어로 설명해 보세요. 또한 이미지의 밝기 분포가 하나의 봉우리로만 몰려 있을 때 Otsu의 결과가 왜 기대만큼 좋지 않을 수 있는지 생각해 보세요.
3. 조명이 불균일한 이미지에 전역 Otsu 이진화와 `adaptiveThreshold`를 각각 적용하고 결과를 비교해 보세요. `blockSize`를 여러 값(예: 11, 25, 51)으로 바꿔가며 결과가 어떻게 달라지는지도 관찰해 보세요.
4. `ADAPTIVE_THRESH_MEAN_C`와 `ADAPTIVE_THRESH_GAUSSIAN_C`의 결과를 같은 이미지에서 비교하고 두 방법의 차이를 설명해 보세요.
5. 본문의 `binarize_document` 예제에서 미디안 블러 대신 가우시안 블러를 사용하도록 바꿔보고, 문서 이미지의 이진화 품질이 어떻게 달라지는지 확인해 보세요.

---

[◀ 이전: 9장. 필터링과 블러](ch09-필터링과블러.md) | [📖 목차](00-목차.md) | [다음: 11장. 모폴로지 연산 ▶](ch11-모폴로지연산.md)
